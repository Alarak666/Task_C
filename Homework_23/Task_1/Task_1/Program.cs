using System;

namespace Task_1
{
    class Program
    {
        static void Main(string[] args)
        {
            string a =(Console.ReadLine());
            char[] array = new char[] { };
            Console.WriteLine(a);
            for (int i = 0; i < a.Length; i++)
            {
                if(a.Length > 0)
                {

                    array[i]; 
                }

            }
        }
    }
}
